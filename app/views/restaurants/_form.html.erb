<div class="container">
  <h3 class="mt-3">Your review</h3>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating, placeholder: "From 1 to 5" %>
    <%= f.input :content %>
    <%= f.submit "Submit review", class: "btn btn-primary" %>
  <% end %>
</div>
